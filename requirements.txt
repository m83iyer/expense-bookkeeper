# expense-bookkeeper — runtime dependencies
# Python 3.10+
gspread>=6.2.1
google-auth>=2.56.0
PyYAML>=6.0.3
python-dateutil>=2.9.0.post0
requests>=2.34.2

# Gmail adapter dependencies — required by scripts/adapters/gmail_auth.py
# Only invoked if you pick the Gmail capture (email_gmail) or Gmail confirmation
# adapter at setup. Bundled in the default install so the advertised Gmail
# path works out of the box; harmless if you never use Gmail.
google-auth-oauthlib>=1.4.0
google-api-python-client>=2.200.0
